require 'spec_helper'

describe MultipartForm::InProgressForm do
  describe "validations" do
    before(:each) do

    end


    it "should require a form subject"
    it "should require a form name"
    it "should require a last completed step"
    it "should require completed"
    it "should require the form name be present in the acts_as_multipart_form parameters for the form subject"
  end

  it "should default to not complete"

end
